#include <iostream>
#include <cmath>
using namespace std;
int main()
{
    float x,y,z;
    cout<<"Enter the first number: ";
    cin>>x;
    cout<<"Enter the second number: ";
    cin>>y;
    z= pow(x,y);
    cout<<" the power of "<<x<<" and "<<y<<" is "<<z;
    return 0;
}
